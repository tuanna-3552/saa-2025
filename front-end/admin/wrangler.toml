name = "saa-admin-35f"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[vars]
# Non-secret environment variables (add secrets via Cloudflare dashboard)
NEXT_PUBLIC_APP_ENV = "production"
