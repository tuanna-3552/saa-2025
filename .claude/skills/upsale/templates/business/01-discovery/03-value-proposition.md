# Value Proposition — {PRODUCT_NAME}
**Use context:** {internal|hybrid|customer-facing}

- **Top stated benefits (3–5, quoted directly):**
  - "{benefit 1}" — `{spec-path:line}`
  - "{benefit 2}" — `{spec-path:line}`
  - "{benefit 3}" — `{spec-path:line}`
- **Jobs-to-be-done language:** {quote from spec/README, or `(no evidence in spec)`}
- **Claimed differentiators (only if explicit — do NOT infer):**
  - "{differentiator}" — `{spec-path:line}`

<!-- Total length under 80 lines. Snapshot only — no recommendations or commentary. -->
