# Scale & Complexity Signals — {PROJECT_NAME}
**Use context:** {internal|hybrid|customer-facing}

- **Approximate LOC per major language:**
  - {language}: ~{N} LOC
- **Git history signals:**
  - Commit count: ~{N}
  - Contributor count: ~{N}
  - First commit: {YYYY-MM-DD}
  - Last commit: {YYYY-MM-DD}
- **Files exceeding 200 LOC (top 10 by LOC):**
  - `{path}` — {N} LOC
- **TODO/FIXME/XXX density:** ~{N} per 1k LOC

<!-- Total length under 100 lines. Snapshot only — no narration. -->
