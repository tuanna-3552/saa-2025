# Platform Support — {PROJECT_NAME}
**Use context:** {internal|hybrid|customer-facing}

- **Client delivery modes:**
  - Web app: {SPA | SSR | MPA | (none detected)} — `{path:line}`
  - Mobile: {React Native | Flutter | Capacitor | native iOS·Android | (none detected)} — `{path:line}`
  - Desktop: {Electron | Tauri | native | (none detected)} — `{path:line}`
  - CLI: {detected | (none detected)} — `{path:line}` (`bin` field, `console_scripts`, `cobra`, `urfave/cli`)
  - Public API or SDK: {OpenAPI spec at `{path}` | SDK package at `{path}` | (none detected)}
- **Deployment modes:**
  - Self-hosted / on-premise: {Dockerfile | Helm | Terraform | Ansible | (none detected)} — `{path:line}`
  - Cloud-managed SaaS: {Vercel | Netlify | Render | Railway | (none detected)} — `{path:line}`
  - Serverless: {Cloudflare Workers | AWS Lambda | GCP Cloud Functions | (none detected)} — `{path:line}`
  - PaaS: {Procfile | (none detected)} — `{path:line}`
  - Multi-tenant / single-tenant signals: {description} (`{path:line}`)
- **OS / runtime matrix:** {`strategy.matrix.os` entries | Docker base-image OS | README badge | (none)} (`{path:line}`)

<!-- Total length under 150 lines. Snapshot only — no narration. -->
