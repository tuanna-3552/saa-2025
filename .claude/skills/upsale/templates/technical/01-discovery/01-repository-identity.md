# Repository Identity — {PROJECT_NAME}
**Use context:** {internal|hybrid|customer-facing}
<!-- REQUIRED: copy verbatim from `plans/upsale/use-context.json#useContext`. Every per-item discovery file emits the same marker on line 2 so downstream steps can read it from any one. -->

- **Project name:** {from `package.json` / `pyproject.toml` / `Cargo.toml` / `go.mod` / top-level `README.md`}
- **One-sentence purpose:** {from README first paragraph or spec frontmatter}
- **Primary language(s):** {language} {version} (cite manifest `path:line`)
- **Runtime:** {Node | Python | JVM | Go | …} {version} (cite `path:line`)

<!-- Total length under 60 lines. Snapshot only — no narration. -->
