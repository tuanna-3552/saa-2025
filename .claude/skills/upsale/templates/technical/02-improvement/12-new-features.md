# Improvement Aspect: New Features — {PROJECT_NAME}
**Use context:** {internal|hybrid|customer-facing}
<!-- REQUIRED: copy verbatim from any 01-discovery/ item line 2. Parsed by Step 4.3 and Phase D pre-extraction. Do NOT re-classify. -->

<!-- Net-new stack-supported capabilities; 3–8 items when discovery supports. Use-context conditionals — see references/technical/02-improvement.md (Shared rules + Aspect 12). -->

<!-- One or more entries below. Empty aspect emits a single 'Status: clean — no current gap' entry. -->

{entries — see Entry format below}

---

## Entry format

```markdown
- Status: {opportunity | clean — no current gap | needs-more-discovery}
- Category: new-features
- Observation: {1-2 sentences tied directly to the discovery snapshot — cite the stack signal or gap that makes this feasible}
- Evidence: {quote specific bullet(s) from discovery; include path:line, metric, or node ID}
- Potential improvement: {1-3 sentences, technology-consistent with stack}
- Customer-value signal: {categorical tag — vocabulary in reference}
- Value: {high | medium | low — rubric in reference}
- Effort hint: {low | medium | high} — based on stack fit and scope, not guessing
- Risk if untouched: {what breaks, slows, or costs the customer if we do nothing}
```

<!-- Total length under 200 lines. Single-aspect scope only — no cross-aspect ranking. -->
