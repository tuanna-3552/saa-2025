---
description: Generate a focused customer upsale proposal for the current repository
argument-hint: [prompt]
---

Read `.claude/skills/upsale/SKILL.md` end-to-end and follow the workflow exactly.

<prompt>$ARGUMENTS</prompt>

The optional `<prompt>` biases prioritization only — it never replaces the full investigation.
