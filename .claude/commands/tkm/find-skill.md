---
description: Discover and install agent skills by keyword or task description
argument-hint: [search query]
---

Activate the `find-skills` skill.

<query>$ARGUMENTS</query>

Search local skill catalog and registry. Show matching skills with:
- Name, description
- Required dependencies
- Install command (if not already available)

If no local match, suggest closest alternative or recommend creating one via `skill-creator`.
